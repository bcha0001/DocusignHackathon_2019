<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.bryan.com.docusignhackathon_forestapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/forest"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".DocuSignActivity"
            android:label="@string/title_activity_docu_sign"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginSignup.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginSignup.SignupActivity" />
        <activity android:name=".AppActivity" />
        <activity android:name=".SinglePetitionActivity"></activity>
    </application>

</manifest>